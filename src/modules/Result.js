import React from 'react';

function Result(props) {
    return (
        <div className='modal-result'>
            The {props.winner} is the winner        
        </div>
    );
}

export default Result;